# PR10: Visual Guidance - Quick Start Guide

## شروع سریع در 5 دقیقه ⚡

این راهنما به شما کمک می‌کند تا در کمتر از 5 دقیقه با قابلیت‌های جدید هدایت بصری آشنا شوید.

## پیش‌نیازها

✅ WordPress 6.0+  
✅ PHP 8.2+  
✅ Node.js 16+ (برای build)  
✅ Homaye Tabesh Plugin نصب شده باشد

## مرحله 1: Build و نصب (2 دقیقه)

```bash
# رفتن به دایرکتوری افزونه
cd wp-content/plugins/homaye-tabesh

# نصب dependencies
npm install

# Build کردن React components
npm run build
```

✨ همین! اکنون PR10 آماده است.

## مرحله 2: تست هدایت بصری (1 دقیقه)

### روش 1: از Console
1. صفحه‌ای از سایت را باز کنید
2. Console را باز کنید (F12)
3. این کد را اجرا کنید:

```javascript
window.Homa.emit('visual:action', {
    command: 'HIGHLIGHT',
    target_selector: 'h1',
    duration: 5000
});
```

✅ اولین تیتر صفحه باید با افکت نور پالس‌دار هایلایت شود!

### روش 2: Tooltip
```javascript
window.Homa.emit('visual:action', {
    command: 'SHOW_TOOLTIP',
    target_selector: 'h1',
    message: 'این یک تولتیپ آزمایشی است! 👋',
    duration: 10000
});
```

✅ یک تولتیپ زیبا روی تیتر ظاهر می‌شود!

## مرحله 3: بررسی Explore Widget (30 ثانیه)

1. سایدبار هما را باز کنید (FAB در گوشه صفحه)
2. اگر چت خالی است، ویجت اکسپلور را می‌بینید
3. روی کارت‌های پیشنهادی کلیک کنید

💡 **نکته**: اگر ویجت خالی است، کمی در سایت بگردید تا علایق شما ثبت شود.

## مرحله 4: تست پیام زنده ادمین (1.5 دقیقه)

### قسمت A: آماده‌سازی (20 ثانیه)
1. از یک مرورگر معمولی، سایت را باز کنید
2. سایدبار هما را باز کنید
3. مرورگر را باز نگه دارید

### قسمت B: ارسال پیام (1 دقیقه)
1. از مرورگر دیگر، وارد پنل ادمین شوید
2. بروید به: **همای تابش > 💬 مداخله زنده**
3. جلسه فعال را انتخاب کنید (لیست سمت چپ)
4. پیامی بنویسید، مثلاً: "سلام! من ادمین هستم 👋"
5. (اختیاری) یک selector CSS اضافه کنید، مثلاً: `.site-header`
6. **ارسال پیام** را بزنید

### قسمت C: بررسی (10 ثانیه)
به مرورگر اول برگردید.

✅ پیام باید بدون رفرش در چت ظاهر شود!  
✅ اگر selector اضافه کردید، باید هایلایت هم اجرا شود!

## مرحله 5: تست با Gemini (اختیاری)

اگر Gemini API Key دارید:

1. به **تنظیمات > همای تابش** بروید
2. API Key را وارد کنید
3. از سایدبار هما بپرسید: "چطوری خرید کنم؟"

✅ هما باید همزمان پاسخ متنی بدهد و دکمه‌های مرتبط را هایلایت کند!

## چک‌لیست نهایی ✓

بعد از این 5 مرحله، باید این‌ها کار کنند:

- [x] هایلایت و Glow روی المان‌ها
- [x] تولتیپ‌های تعاملی
- [x] ویجت اکسپلور با پیشنهادات
- [x] پیام‌رسانی Real-time بین ادمین و کاربر
- [x] (اختیاری) دستورات بصری از Gemini

## دستورات مفید

### Debug Console
```javascript
// بررسی وضعیت event bus
window.Homa.checkConnectivity();

// تاریخچه eventها
window.Homa.getEventHistory();

// وضعیت Visual Guidance
console.log(window.HomaVisualGuidance);

// تعداد افکت‌های فعال
window.HomaDebug.VisualGuidance.getActiveCount();

// پاک کردن همه افکت‌ها
window.HomaDebug.VisualGuidance.clearAll();
```

### Test Commands
```javascript
// 1. Highlight با scroll
window.Homa.emit('visual:action', {
    command: 'HIGHLIGHT',
    target_selector: 'footer',
    duration: 5000
});

// 2. Pulse effect
window.Homa.emit('visual:action', {
    command: 'PULSE',
    target_selector: '.logo',
    duration: 5000
});

// 3. Glow effect
window.Homa.emit('visual:action', {
    command: 'GLOW',
    target_selector: '.menu',
    duration: 5000
});
```

## مشکلات متداول

### ❌ "HomaVisualGuidance is not defined"
**راه‌حل**: صفحه را رفرش کنید. اسکریپت‌ها باید لود شوند.

### ❌ ویجت اکسپلور خالی است
**راه‌حل**: کمی در سایت بگردید (چند صفحه محصول ببینید).

### ❌ پیام ادمین دریافت نمی‌شود
**بررسی‌ها**:
1. آیا در لیست جلسات فعال هستید؟
2. آیا Cookie ها فعال هستند؟
3. Console را چک کنید برای خطاها

### ❌ Build خطا می‌دهد
```bash
# پاک کردن و نصب مجدد
rm -rf node_modules package-lock.json
npm install
npm run build
```

## منابع بیشتر

📖 [Implementation Guide](PR10-IMPLEMENTATION.md) - جزئیات فنی کامل  
📖 [README](PR10-README.md) - مستندات عمومی  
🐛 [GitHub Issues](https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh/issues) - گزارش مشکلات

## مراحل بعدی

حالا که PR10 را تست کردید، می‌توانید:

1. **سفارشی‌سازی CSS**: رنگ‌ها و انیمیشن‌ها را تغییر دهید
2. **افزودن Selectors**: المان‌های خاص سایت خود را target کنید
3. **Training Gemini**: به Gemini یاد بدهید چه موقع از visual commands استفاده کند
4. **Monitor Analytics**: ببینید کاربران چقدر با افکت‌ها تعامل دارند

## کمک نیاز دارید؟

💬 سوال دارید؟ یک Issue باز کنید!  
🐛 باگ پیدا کردید؟ با جزئیات گزارش دهید!  
💡 ایده دارید؟ در Discussions به اشتراک بگذارید!

---

**موفق باشید! 🚀**

این راهنما آخرین بار در: December 26, 2024 به‌روزرسانی شد.
