# راهنمای نصب همای تابش

## پیش‌نیازها

قبل از نصب، مطمئن شوید که سیستم شما این موارد را دارد:

- ✅ PHP نسخه 8.2 یا بالاتر
- ✅ WordPress نسخه 6.0 یا بالاتر
- ✅ Composer (برای نصب وابستگی‌ها)
- ✅ کلید API گوگل Gemini (رایگان از [Google AI Studio](https://makersuite.google.com/app/apikey))

### اختیاری (برای بهترین تجربه)
- 🎨 قالب Divi برای ردیابی خودکار المان‌ها
- 🛒 افزونه WooCommerce برای Context محصولات

## مراحل نصب

### 1. دانلود و استقرار

```bash
# روش اول: از طریق Git
cd /path/to/wordpress/wp-content/plugins/
git clone https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh.git homaye-tabesh

# روش دوم: دانلود ZIP و استخراج
# فایل ZIP را دانلود کرده و در پوشه plugins استخراج کنید
```

### 2. نصب وابستگی‌ها

```bash
cd homaye-tabesh
composer install --no-dev --optimize-autoloader
```

اگر Composer نصب نیست:
```bash
# لینوکس/macOS
curl -sS https://getcomposer.org/installer | php
php composer.phar install --no-dev --optimize-autoloader

# ویندوز
# از https://getcomposer.org/download/ نصب کنید
```

### 3. فعال‌سازی افزونه

1. به پنل مدیریت WordPress بروید
2. از منوی سمت چپ، **افزونه‌ها** > **افزونه‌های نصب شده** را انتخاب کنید
3. افزونه **همای تابش** را پیدا کرده و روی **فعال‌سازی** کلیک کنید

### 4. دریافت کلید API

1. به [Google AI Studio](https://makersuite.google.com/app/apikey) بروید
2. با حساب گوگل خود وارد شوید
3. روی **Create API Key** کلیک کنید
4. کلید ایجاد شده را کپی کنید

### 5. تنظیمات اولیه

1. در پنل WordPress، به **همای تابش** > **تنظیمات** بروید
2. کلید API را در فیلد مربوطه وارد کنید
3. گزینه‌های دیگر را بر اساس نیاز تنظیم کنید:
   - ✅ **ردیابی رفتار**: برای فعال‌سازی Telemetry
   - ✅ **یکپارچه‌سازی با Divi**: اگر از قالب Divi استفاده می‌کنید
   - 🔢 **حداقل امتیاز پرسونا**: پیش‌فرض 50
4. روی **ذخیره تنظیمات** کلیک کنید

## بررسی نصب

### وضعیت سیستم

در صفحه تنظیمات، جدول **وضعیت سیستم** را بررسی کنید:

```
✅ نسخه PHP: 8.2.x
✅ نسخه WordPress: 6.x
✅ قالب فعال: Divi
✅ WooCommerce: نصب شده
✅ API Key تنظیم شده: بله
```

### تست عملکرد

#### 1. بررسی ایجاد جداول دیتابیس

```sql
-- در phpMyAdmin یا MySQL Console
SHOW TABLES LIKE '%homaye%';

-- باید این دو جدول را ببینید:
-- wp_homaye_persona_scores
-- wp_homaye_telemetry_events
```

#### 2. بررسی REST API

در مرورگر یا با cURL:

```bash
# بررسی endpoint
curl -X POST https://your-site.com/wp-json/homaye/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"event_type":"test","element_class":"test","element_data":{}}'
```

انتظار می‌رود پاسخی مانند این دریافت کنید:
```json
{"success":true,"message":"Event recorded"}
```

#### 3. بررسی اسکریپت ردیابی

1. صفحه اصلی سایت را باز کنید
2. Developer Tools مرورگر را باز کنید (F12)
3. در Console باید پیام زیر را ببینید:
```
Homaye Tabesh - Tracking initialized
```

## مشکلات رایج و راه‌حل

### خطا: "Cannot unserialize singleton"
**راه‌حل:** Cache سایت را پاک کنید و صفحه را رفرش کنید.

### خطا: "Class not found"
**راه‌حل:** 
```bash
cd wp-content/plugins/homaye-tabesh
composer dump-autoload --optimize
```

### API Key کار نمی‌کند
**راه‌حل:**
1. مطمئن شوید کلید صحیح کپی شده
2. بررسی کنید که API Key فعال باشد
3. محدودیت‌های IP/Domain را در Google Cloud Console چک کنید

### اسکریپت tracker.js لود نمی‌شود
**راه‌حل:**
1. Cache مرورگر را پاک کنید
2. از Developer Tools، Network tab را بررسی کنید
3. مطمئن شوید فایل در مسیر صحیح قرار دارد

### رویدادها ثبت نمی‌شوند
**راه‌حل:**
1. بررسی کنید که در پنل مدیریت لاگین نیستید
2. JavaScript را در مرورگر فعال کنید
3. Console را برای خطاهای JavaScript بررسی کنید

## حذف (Uninstall)

### حذف کامل

```bash
# 1. غیرفعال‌سازی از پنل WordPress
# 2. حذف افزونه

# 3. حذف جداول (اختیاری - اگر می‌خواهید داده‌ها هم پاک شوند)
# در phpMyAdmin یا MySQL Console:
DROP TABLE IF EXISTS wp_homaye_persona_scores;
DROP TABLE IF EXISTS wp_homaye_telemetry_events;

# 4. حذف options
DELETE FROM wp_options WHERE option_name LIKE 'ht_%';
```

## ارتقا (Upgrade)

### نسخه‌های آینده

```bash
cd wp-content/plugins/homaye-tabesh
git pull origin main
composer install --no-dev --optimize-autoloader

# سپس در پنل WordPress:
# افزونه‌ها > غیرفعال کنید > فعال کنید (برای اجرای migration)
```

## پشتیبانی

- 📧 ایمیل: [ایجاد Issue در GitHub](https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh/issues)
- 📚 مستندات: [README.md](README.md)
- 💻 نمونه کد: [examples/usage-examples.php](examples/usage-examples.php)

## بعد از نصب

حالا می‌توانید:
1. صفحه **آمار پرسونا** را بررسی کنید
2. از نمونه کدهای `examples/usage-examples.php` استفاده کنید
3. المان‌های سفارشی با `data-homaye-track` اضافه کنید
4. قوانین پایگاه دانش را در `knowledge-base/` سفارشی کنید

موفق باشید! 🚀
