# راهنمای نصب همای تابش

## انتخاب روش نصب مناسب

### 🎯 کاربران نهایی (توصیه شده)

اگر فقط می‌خواهید از افزونه استفاده کنید (بدون توسعه یا تغییر کد):

**✅ از نسخه Release استفاده کنید** - نیازی به نصب Composer یا npm ندارد

### 👨‍💻 توسعه‌دهندگان

اگر می‌خواهید روی کد کار کنید، ویژگی اضافه کنید یا باگ رفع کنید:

**✅ از سورس کد استفاده کنید** - نیاز به نصب Composer و npm دارد

---

## روش ۱: نصب از Release (برای کاربران نهایی)

این روش **ساده‌ترین** و **سریع‌ترین** روش نصب است.

### مراحل نصب

1. **دانلود نسخه آماده**
   - به صفحه [Releases](https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh/releases) بروید
   - آخرین نسخه (مثلاً `v1.0.0`) را پیدا کنید
   - فایل `homaye-tabesh-v1.0.0.zip` را دانلود کنید

2. **نصب در وردپرس**
   - به پنل مدیریت WordPress بروید
   - از منو: **افزونه‌ها** > **افزودن**
   - روی **بارگذاری افزونه** کلیک کنید
   - فایل ZIP دانلود شده را انتخاب و آپلود کنید
   - روی **هم‌اکنون نصب کن** کلیک کنید

3. **فعال‌سازی**
   - بعد از نصب، روی **فعال‌سازی افزونه** کلیک کنید

4. **تنظیمات API**
   - به [Google AI Studio](https://makersuite.google.com/app/apikey) بروید
   - یک API Key رایگان بگیرید
   - در وردپرس: **همای تابش** > **تنظیمات**
   - کلید API را وارد کنید و ذخیره کنید

✅ **تمام!** افزونه آماده استفاده است.

---

## روش ۲: نصب از سورس (برای توسعه‌دهندگان)

این روش برای کسانی است که می‌خواهند روی کد کار کنند.

### پیش‌نیازها

قبل از شروع، این موارد را نصب کنید:

- ✅ PHP نسخه 8.2 یا بالاتر
- ✅ WordPress نسخه 6.0 یا بالاتر  
- ✅ Composer - [نصب Composer](https://getcomposer.org/download/)
- ✅ Node.js و npm - [نصب Node.js](https://nodejs.org/)
- ✅ Git - [نصب Git](https://git-scm.com/downloads)

### مراحل نصب

1. **کلون مخزن**

```bash
# روش اول: از طریق Git
cd /path/to/wordpress/wp-content/plugins/
git clone https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh.git homaye-tabesh
cd homaye-tabesh
```

2. **نصب وابستگی‌های PHP**

```bash
# نصب با Composer
composer install --no-dev --optimize-autoloader
```

اگر Composer نصب نیست:
```bash
# لینوکس/macOS
curl -sS https://getcomposer.org/installer | php
php composer.phar install --no-dev --optimize-autoloader

# ویندوز
# از https://getcomposer.org/download/ نصب کنید
```

3. **نصب وابستگی‌های JavaScript (اختیاری)**

```bash
# نصب بسته‌های npm
npm install

# ساخت فایل‌های production
npm run build
```

> **نکته**: اگر `npm install` اجرا نکنید، افزونه همچنان کار می‌کند اما ممکن است برخی ویژگی‌های frontend محدود باشند.

4. **فعال‌سازی افزونه**

1. به پنل مدیریت WordPress بروید
2. از منوی سمت چپ، **افزونه‌ها** > **افزونه‌های نصب شده** را انتخاب کنید
3. افزونه **همای تابش** را پیدا کرده و روی **فعال‌سازی** کلیک کنید

5. **دریافت کلید API**

1. به [Google AI Studio](https://makersuite.google.com/app/apikey) بروید
2. با حساب گوگل خود وارد شوید
3. روی **Create API Key** کلیک کنید
4. کلید ایجاد شده را کپی کنید

6. **تنظیمات اولیه**

1. در پنل WordPress، به **همای تابش** > **تنظیمات** بروید
2. کلید API را در فیلد مربوطه وارد کنید
3. گزینه‌های دیگر را بر اساس نیاز تنظیم کنید:
   - ✅ **ردیابی رفتار**: برای فعال‌سازی Telemetry
   - ✅ **یکپارچه‌سازی با Divi**: اگر از قالب Divi استفاده می‌کنید
   - 🔢 **حداقل امتیاز پرسونا**: پیش‌فرض 50
4. روی **ذخیره تنظیمات** کلیک کنید

### اختیاری (برای بهترین تجربه)
- 🎨 قالب Divi برای ردیابی خودکار المان‌ها
- 🛒 افزونه WooCommerce برای Context محصولات

---

## چگونه autoloader کار می‌کند؟

### برای کاربران نهایی (بدون Composer)

هنگامی که افزونه را از Release دانلود می‌کنید:
- پوشه `vendor/` وجود ندارد
- افزونه به صورت خودکار از **autoloader داخلی** استفاده می‌کند (`includes/autoload.php`)
- این autoloader تمام کلاس‌های `HomayeTabesh\*` را از پوشه `includes/` بارگذاری می‌کند
- **هیچ خطایی رخ نمی‌دهد** و افزونه به درستی کار می‌کند

### برای توسعه‌دهندگان (با Composer)

هنگامی که `composer install` را اجرا می‌کنید:
- پوشه `vendor/` ایجاد می‌شود
- افزونه از **Composer autoloader** استفاده می‌کند (`vendor/autoload.php`)
- این روش بهینه‌تر و سریع‌تر است
- مناسب برای محیط development

### مکانیزم Fallback

فایل اصلی افزونه (`homaye-tabesh.php`) به صورت هوشمند انتخاب می‌کند:

```php
if (file_exists(HT_PLUGIN_DIR . 'vendor/autoload.php')) {
    // استفاده از Composer autoloader (اگر موجود باشد)
    require_once HT_PLUGIN_DIR . 'vendor/autoload.php';
} else {
    // استفاده از autoloader داخلی (برای کاربران نهایی)
    require_once HT_PLUGIN_DIR . 'includes/autoload.php';
}
```

این طراحی تضمین می‌کند که افزونه در هر دو حالت کار کند! 🎉

---

### وضعیت سیستم

در صفحه تنظیمات، جدول **وضعیت سیستم** را بررسی کنید:

```
✅ نسخه PHP: 8.2.x
✅ نسخه WordPress: 6.x
✅ قالب فعال: Divi
✅ WooCommerce: نصب شده
✅ API Key تنظیم شده: بله
```

### تست عملکرد

#### 1. بررسی ایجاد جداول دیتابیس

```sql
-- در phpMyAdmin یا MySQL Console
SHOW TABLES LIKE '%homaye%';

-- باید این دو جدول را ببینید:
-- wp_homaye_persona_scores
-- wp_homaye_telemetry_events
```

#### 2. بررسی REST API

در مرورگر یا با cURL:

```bash
# بررسی endpoint
curl -X POST https://your-site.com/wp-json/homaye/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"event_type":"test","element_class":"test","element_data":{}}'
```

انتظار می‌رود پاسخی مانند این دریافت کنید:
```json
{"success":true,"message":"Event recorded"}
```

#### 3. بررسی اسکریپت ردیابی

1. صفحه اصلی سایت را باز کنید
2. Developer Tools مرورگر را باز کنید (F12)
3. در Console باید پیام زیر را ببینید:
```
Homaye Tabesh - Tracking initialized
```


## بررسی نصب

### وضعیت سیستم

در صفحه تنظیمات، جدول **وضعیت سیستم** را بررسی کنید:

```
✅ نسخه PHP: 8.2.x
✅ نسخه WordPress: 6.x
✅ قالب فعال: Divi
✅ WooCommerce: نصب شده
✅ API Key تنظیم شده: بله
✅ Autoloader: فعال (Composer یا Fallback)
```

### تست عملکرد

#### 1. بررسی ایجاد جداول دیتابیس

```sql
-- در phpMyAdmin یا MySQL Console
SHOW TABLES LIKE '%homaye%';

-- باید این دو جدول را ببینید:
-- wp_homaye_persona_scores
-- wp_homaye_telemetry_events
```

#### 2. بررسی REST API

در مرورگر یا با cURL:

```bash
# بررسی endpoint
curl -X POST https://your-site.com/wp-json/homaye/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"event_type":"test","element_class":"test","element_data":{}}'
```

انتظار می‌رود پاسخی مانند این دریافت کنید:
```json
{"success":true,"message":"Event recorded"}
```

#### 3. بررسی اسکریپت ردیابی

1. صفحه اصلی سایت را باز کنید
2. Developer Tools مرورگر را باز کنید (F12)
3. در Console باید پیام زیر را ببینید:
```
Homaye Tabesh - Tracking initialized
```

---

## مشکلات رایج و راه‌حل

### ❌ خطا: "Failed opening required vendor/autoload.php"

این خطا زمانی رخ می‌دهد که:
- از نسخه قدیمی افزونه استفاده می‌کنید که autoloader fallback ندارد
- فایل `includes/autoload.php` حذف شده است

**راه‌حل:**
1. از آخرین نسخه Release استفاده کنید
2. یا اگر از سورس استفاده می‌کنید، `composer install` را اجرا کنید

### ❌ خطا: "Class 'HomayeTabesh\HT_Core' not found"

**راه‌حل:**
1. مطمئن شوید فایل `includes/HT_Core.php` وجود دارد
2. اگر از Composer استفاده می‌کنید:
   ```bash
   cd wp-content/plugins/homaye-tabesh
   composer dump-autoload --optimize
   ```
3. Cache افزونه را پاک کنید

### خطا: "Cannot unserialize singleton"
### خطا: "Cannot unserialize singleton"

**راه‌حل:** Cache سایت را پاک کنید و صفحه را رفرش کنید.

### خطا: "Class not found" (پس از بروزرسانی)

**راه‌حل:** 
```bash
cd wp-content/plugins/homaye-tabesh
composer dump-autoload --optimize
```

### API Key کار نمی‌کند
**راه‌حل:**
1. مطمئن شوید کلید صحیح کپی شده
2. بررسی کنید که API Key فعال باشد
3. محدودیت‌های IP/Domain را در Google Cloud Console چک کنید

### اسکریپت tracker.js لود نمی‌شود
**راه‌حل:**
1. Cache مرورگر را پاک کنید
2. از Developer Tools، Network tab را بررسی کنید
3. مطمئن شوید فایل در مسیر صحیح قرار دارد

### رویدادها ثبت نمی‌شوند
**راه‌حل:**
1. بررسی کنید که در پنل مدیریت لاگین نیستید
2. JavaScript را در مرورگر فعال کنید
3. Console را برای خطاهای JavaScript بررسی کنید

## حذف (Uninstall)

### حذف کامل

```bash
# 1. غیرفعال‌سازی از پنل WordPress
# 2. حذف افزونه

# 3. حذف جداول (اختیاری - اگر می‌خواهید داده‌ها هم پاک شوند)
# در phpMyAdmin یا MySQL Console:
DROP TABLE IF EXISTS wp_homaye_persona_scores;
DROP TABLE IF EXISTS wp_homaye_telemetry_events;

# 4. حذف options
DELETE FROM wp_options WHERE option_name LIKE 'ht_%';
```

## ارتقا (Upgrade)

### نسخه‌های آینده

```bash
cd wp-content/plugins/homaye-tabesh
git pull origin main
composer install --no-dev --optimize-autoloader

# سپس در پنل WordPress:
# افزونه‌ها > غیرفعال کنید > فعال کنید (برای اجرای migration)
```

## پشتیبانی

- 📧 ایمیل: [ایجاد Issue در GitHub](https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh/issues)
- 📚 مستندات: [README.md](README.md)
- 💻 نمونه کد: [examples/usage-examples.php](examples/usage-examples.php)

## بعد از نصب

حالا می‌توانید:
1. صفحه **آمار پرسونا** را بررسی کنید
2. از نمونه کدهای `examples/usage-examples.php` استفاده کنید
3. المان‌های سفارشی با `data-homaye-track` اضافه کنید
4. قوانین پایگاه دانش را در `knowledge-base/` سفارشی کنید

موفق باشید! 🚀
