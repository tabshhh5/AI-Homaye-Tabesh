# PR15 Quick Start Guide

## راهنمای سریع: تشخیص هویت چندسطحی و شناسایی مهاجم

🚀 این راهنما به شما کمک می‌کند تا در چند دقیقه با قابلیت‌های جدید PR15 آشنا شوید.

---

## 📥 نصب و فعال‌سازی

### مرحله 1: بروزرسانی افزونه
```bash
cd wp-content/plugins/homaye-tabesh
git pull origin main
composer install
npm install
npm run build
```

### مرحله 2: فعال‌سازی خودکار
- جداول دیتابیس به صورت خودکار در اولین بارگذاری ایجاد می‌شوند
- Cron Job برای پاکسازی لاگ‌های قدیمی تنظیم می‌شود
- REST API Endpoints به صورت خودکار ثبت می‌شوند

---

## 🎯 استفاده سریع

### 1. تست نقش مدیر
```
1. با اکانت مدیریت وارد شوید
2. دکمه FAB هما را کلیک کنید
3. بخش ابزارهای مدیریتی را مشاهده کنید:
   - آمار امروز
   - کاربران آنلاین
   - هشدارهای امنیتی
   - داشبورد اطلس
```

### 2. تست نقش مشتری
```
1. با اکانت مشتری وارد شوید
2. هما سفارشات شما را نمایش می‌دهد
3. دکمه‌های موجود:
   - پیگیری آخرین سفارش
   - تیکت پشتیبانی
   - تمدید فاکتور
```

### 3. تست نقش میهمان
```
1. در حالت لاگ‌اوت
2. پیام خوش‌آمدگویی نمایش داده می‌شود
3. دکمه‌های موجود:
   - معرفی خدمات چاپکو
   - محاسبه تیراژ کتاب
   - ثبت‌نام سریع
```

### 4. تست شناسایی مهاجم
```
⚠️ هشدار: این تست دسترسی شما را موقتاً مسدود می‌کند!

1. آدرس زیر را باز کنید:
   https://yoursite.com/wp-config.php

2. یا در چت بنویسید:
   <script>alert('test')</script>

3. هما شما را به عنوان مهاجم شناسایی می‌کند
4. برای بازگشت به حالت عادی، صفحه را رفرش کنید
```

---

## 🔍 بررسی عملکرد

### صفحه تست
باز کنید: `https://yoursite.com/wp-content/plugins/homaye-tabesh/validate-pr15.html`

### بررسی REST API
```bash
# دریافت نقش کاربر جاری
curl https://yoursite.com/wp-json/homaye-tabesh/v1/capabilities/context

# دریافت ابزارهای موجود
curl https://yoursite.com/wp-json/homaye-tabesh/v1/capabilities/tools

# دریافت هشدارهای امنیتی (فقط مدیر)
curl https://yoursite.com/wp-json/homaye-tabesh/v1/security/alerts
```

---

## 🛠️ تنظیمات پیشرفته

### افزودن IP به Whitelist
```php
$intruder_detector = HT_Core::instance()->intruder_detector;
$intruder_detector->add_to_whitelist('192.168.1.100');
```

### تغییر آستانه تشخیص مهاجم
فایل `HT_Intruder_Pattern_Matcher.php` خط 41:
```php
private const SCORE_THRESHOLD = 100; // کاهش دهید برای حساسیت بیشتر
```

### افزودن الگوی مشکوک جدید
فایل `HT_Intruder_Pattern_Matcher.php` خط 27:
```php
private const SUSPICIOUS_PATTERNS = [
    // ... الگوهای موجود
    'your_custom_pattern',
];
```

---

## 🎨 سفارشی‌سازی UI

### تغییر پیام خوش‌آمدگویی
فایل `HT_Dynamic_Chat_Capabilities.php` خط 295:
```php
$messages = [
    'admin' => "پیام سفارشی شما برای مدیر",
    'customer' => "پیام سفارشی شما برای مشتری",
    // ...
];
```

### تغییر رنگ Role Badge
فایل `assets/css/homa-role-ui.css` خط 14:
```css
.homa-role-badge.admin {
    background: linear-gradient(135deg, #YOUR_COLOR 0%, #YOUR_COLOR 100%);
}
```

---

## 🐛 عیب‌یابی

### مشکل: نقش کاربر تشخیص داده نمی‌شود
**راهکار:**
```
1. بررسی کنید که کاربر لاگین است: is_user_logged_in()
2. Cache مرورگر را پاک کنید
3. نشست PHP را بررسی کنید
```

### مشکل: هشدارهای امنیتی نمایش داده نمی‌شوند
**راهکار:**
```
1. بررسی کنید که با اکانت مدیر لاگین کرده‌اید
2. جدول security_log را در دیتابیس چک کنید
3. WP_DEBUG را فعال کنید و لاگ‌ها را بررسی کنید
```

### مشکل: React components لود نمی‌شوند
**راهکار:**
```bash
cd wp-content/plugins/homaye-tabesh
npm run build
# سپس cache مرورگر را پاک کنید
```

---

## 📊 مانیتورینگ

### مشاهده لاگ‌های امنیتی
```sql
SELECT * FROM wp_homa_security_log 
ORDER BY created_at DESC 
LIMIT 50;
```

### بررسی آمار حملات
```sql
SELECT event_type, COUNT(*) as count 
FROM wp_homa_security_log 
GROUP BY event_type;
```

### پاکسازی دستی لاگ‌های قدیمی
```php
$security_alerts = HT_Core::instance()->security_alerts;
$deleted = $security_alerts->cleanup_old_logs(30); // حذف لاگ‌های قدیمی‌تر از 30 روز
```

---

## 🔐 نکات امنیتی

### ✅ انجام دهید:
- IP‌های ثابت خود را به whitelist اضافه کنید
- به صورت منظم لاگ‌های امنیتی را بررسی کنید
- آستانه تشخیص را متناسب با ترافیک سایت تنظیم کنید

### ❌ انجام ندهید:
- آستانه را خیلی پایین نگذارید (False Positive)
- لاگ‌های امنیتی را نادیده نگیرید
- بدون تست، الگوهای جدید اضافه نکنید

---

## 🆘 پشتیبانی

- **گزارش باگ**: [GitHub Issues](https://github.com/tabshhh4-sketch/AI-Homaye-Tabesh/issues)
- **مستندات کامل**: `PR15-README.md`
- **جزئیات پیاده‌سازی**: `PR15-IMPLEMENTATION.md`

---

## ✅ چک‌لیست نهایی

- [ ] افزونه بروزرسانی شده است
- [ ] Build موفق بوده است (`npm run build`)
- [ ] جداول دیتابیس ایجاد شده‌اند
- [ ] تست با نقش‌های مختلف انجام شده است
- [ ] صفحه validate-pr15.html تست شده است
- [ ] هشدارهای امنیتی در داشبورد نمایش داده می‌شوند

---

**زمان خواندن**: 5 دقیقه  
**سطح مهارت**: مبتدی تا متوسط  
**نسخه**: 1.0.0
