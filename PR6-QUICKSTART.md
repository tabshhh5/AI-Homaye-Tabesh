# PR6 QUICKSTART - Parallel UI

## 🚀 شروع سریع در 5 دقیقه

### قدم ۱: نصب Dependencies (یک‌بار)

```bash
cd wp-content/plugins/homaye-tabesh
npm install
npm run build
```

### قدم ۲: فعال‌سازی

افزونه را در WordPress فعال کنید. همه چیز خودکار است!

### قدم ۳: تست

1. به هر صفحه‌ای از سایت بروید
2. دکمه بنفش پایین سمت چپ را ببینید؟ روی آن کلیک کنید!
3. سایدبار هما باید باز شود 🎉

---

## 💬 ارسال اولین پیام

در سایدبار، یک پیام تایپ کنید:

```
سلام، می‌خواهم کتابم را چاپ کنم
```

هما پاسخ می‌دهد و Smart Chips پیشنهادی نمایش می‌دهد!

---

## 🎯 تست Highlight

کنسول مرورگر را باز کنید (F12) و این را اجرا کنید:

```javascript
// Highlight کردن اولین دکمه
window.HomaOrchestrator.executeOnSite('.et_pb_button', 'highlight');
```

دکمه باید با انیمیشن بنفش highlight شود!

---

## 📋 چک‌لیست تست

- [ ] FAB button نمایش داده می‌شود؟
- [ ] کلیک روی FAB سایدبار را باز می‌کند؟
- [ ] سایت به سمت چپ فشرده می‌شود؟
- [ ] انیمیشن smooth است (بدون پرش)؟
- [ ] می‌توانید در چت تایپ کنید؟
- [ ] می‌توانید روی فرم سایت کلیک کنید؟
- [ ] پس از refresh، چت history حفظ می‌شود؟

---

## 🆘 مشکل دارید؟

### سایدبار باز نمی‌شود

```bash
# مطمئن شوید build شده است
ls -la assets/build/homa-sidebar.js

# اگر فایل نیست، دوباره build کنید
npm run build
```

### خطای JavaScript

کنسول را چک کنید:

```javascript
// اینها باید موجود باشند
console.log(window.HomaOrchestrator);  // object
console.log(window.React);              // function
console.log(window.ReactDOM);           // object
```

### چت پاسخ نمی‌دهد

REST API را چک کنید:

```bash
curl -X POST https://yoursite.com/wp-json/homaye/v1/sidebar/state
```

---

## 🎨 تنظیمات سریع

### تغییر نسبت سایت/سایدبار

فایل: `assets/css/homa-parallel-ui.css`

```css
body.homa-open #homa-site-view {
    flex-basis: 60%;  /* از 70 به 60 تغییر دهید */
}

body.homa-open #homa-sidebar-view {
    flex-basis: 40%;  /* از 30 به 40 تغییر دهید */
}
```

### تغییر رنگ FAB

فایل: `assets/js/homa-fab.js`

```css
background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
/* به جای #667eea و #764ba2 */
```

---

## 📱 تست در موبایل

1. مرورگر را resize کنید (کمتر از 768px)
2. Layout باید عمودی شود
3. سایدبار باید از پایین بیاید

---

## 🔥 ویژگی‌های کول

### 1. Chat History

هر چی بگویید ذخیره می‌شود! حتی بعد از refresh.

### 2. Smart Chips

بر اساس پرسونای شما (نویسنده، ناشر، دانشجو) دکمه‌های متفاوتی نشان می‌دهد.

### 3. Streaming Text

پاسخ هما مثل ChatGPT تایپ می‌شود، نه یکباره!

### 4. Form Sync

هر تغییری در فرم سایت، به سایدبار گزارش می‌شود.

---

## 🎓 مرحله بعدی

حالا که کار کرد، بروید [README کامل](./PR6-README.md) را بخوانید!

---

**Ready to build awesome AI experiences? Let's go! 🚀**
